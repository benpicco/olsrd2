To build the OLSR.org V2 routing agent open a shell:

> cd build
> cmake ..
> make

There are some build variables you can choose to configure building.
We suggest to use ccmake to do this, but the defaults should
be already reasonable.

> cd build
> ccmake ..
